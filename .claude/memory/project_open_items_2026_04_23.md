---
name: 2026-04-23 遗留待办事项
description: 今天大批量上线新功能后的 TODO，需要后续跟进
type: project
---

## 功能 TODO（可以接着做）

- **一键上传 Phase 3**：导出 Excel（按学部/大学院两套模板布局生成 .xlsx，方便给社劳士）。当前「导出 Excel」按钮 disabled 占位
- **一键上传 Phase 4**：管理员增强（批量解锁、批量提醒未提交老师等）

## 数据/业务 TODO

- **75 位 baito 公司归属审核**：今天从时薪统计表批量注册 172 位，其中 75 位不在 3 月給与明細表里（新入职或其它原因），全部默认填了「紫陽花教育」。根据匹配数据，大学院 85%、学部 90% 都在紫陽花，所以这个默认是对的概率很大。但有少数可能实际是世家学舍的，需要日后由 admin 在档案库里人肉调整。
- **卞含章时薪数据异常**：答疑做題 100000円/h、研究計画書修改 20000円/h —— 明显是源表填错（可能是月額被填进时薪字段了）。已按原 Excel 值录入，需要 admin 进档案确认后修正。
- **187 位 baito 未改默认密码**：截至今天仍在用 `123456`。需要系统或 admin 侧催改。现有修改密码功能已做好（侧边栏🔑按钮 / 手机版"密码"按钮）。

## 提交老师名单特殊说明

- **22 位 紫陽花入职表新增老师**（2026-04-23 第二轮批量）：档案里有 电话/邮编/住址/部门，时薪未填。login_id 都是拼音。姓名见 `docs/baito-批量注册-待填.csv` + 给与明細匹配。
- **李桓舟**：单独补建的账号（在給与明細表但漏了时薪表）。login_id=lihuanzhou。
- **汪印**：「宣传」分类，不是标准 baito 部门（大学院/学部/语言类/文书）。建号时 department 设 NULL。admin 需要确认这位老师到底是 baito 还是正/契。

## 登录凭证

- 统一密码：`123456`（第一次登录后让老师自己改）
- admin 自己如果忘密码了，可以直接 SQL `UPDATE auth.users SET encrypted_password = crypt('<新密码>', gen_salt('bf', 10)) WHERE email = '<login_id>@juku.local'`（必须 cost=10，详见 project_bulk_create_auth_users.md）

**How to apply:** 下次开场时扫这个文件快速知道还有什么没收尾的。完成一个划掉一个，项目状态流转见 git log。
